{
  "packs": [
    "spec-driven-product",
    "executable-requirements",
    "flutter",
    "firebase",
    "node"
  ],
  "rules": {},
  "accept": [
    {
      "rule": "pack-declaration",
      "path": ".claudinite-checks.json",
      "reason": "flutter and node genuinely apply here but the pack fingerprints look for a repo-root pubspec.yaml / package.json, while this monorepo keeps them in app/ and functions/ — so detect() can't see them and the declaration is deliberately ahead of the root-only fingerprint. (executable-requirements and firebase need no acceptance: they fingerprint-match on dev/requirements/requirements.md and firebase.json.)"
    }
  ]
}
