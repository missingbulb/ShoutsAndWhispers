# ios pack

Active when the repo has an iOS app target (`ios/Runner/Info.plist`). Stub — no rules captured yet; add durable, project-agnostic practices to `RULES.md` as they
are earned. Expected first source: missingbulb/ShoutsAndWhispers.
